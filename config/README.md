# Configuração

Arquivos `*.example.yaml` desta pasta são modelos. Copie sem o `.example` para usar — as cópias são ignoradas pelo Git porque contêm IDs do seu tenant.

Os perfis de onboarding (departamento/tipo → grupos e licença) chegam na Sprint 3.
